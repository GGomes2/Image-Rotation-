# Image-Rotation
ppmstrans will read an ppm image and perform a transformations on it and return a transformed image to stdout. The program will use the csc411_image crate to read in the image and work with RGB images. 
